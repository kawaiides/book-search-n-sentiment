# steps to run:

- git clone [this repo]
- cd repo
- python -m venv env
- source env/bin/activate
- pip install -r requirements.txt
- make a .env file and populate GEMINI_API_KEY and GEMINI_API_URL
- python main.py
